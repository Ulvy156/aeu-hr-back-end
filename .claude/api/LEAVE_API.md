# Leave API

## Purpose

Leave module contract for requests, approvals, balances, and cancellation.

## Base Endpoint

No public leave API endpoint is implemented yet.

## Auth Requirement

No public endpoint is available yet.

## Permissions

Relevant seeded permissions already exist for future leave APIs:

- `leaves.view_any`
- `leaves.view_own`
- `leaves.create`
- `leaves.cancel`
- `leaves.approve_hr`
- `leaves.reject_hr`
- `leaves.approve_ceo`
- `leaves.reject_ceo`
- `leave_balances.view_any`
- `leave_balances.view_own`

## Endpoint List

- No public leave endpoints are implemented yet.

## Request Body Fields

None documented yet because no leave API route exists.

## Query Parameters

None documented yet because no leave API route exists.

## Response Example

No response contract yet because no leave API route exists.

## Validation Notes

- Do not assume leave request, approval, or balance endpoints exist yet.

## Frontend Notes

- Update this file when leave routes are actually implemented in backend source.
